name: Build Runpod image

on:
  workflow_dispatch:
  push:
    branches:
      - experimental
      - runpod-exp
    paths:
      - Dockerfile.runpod
      - .dockerignore
      - pyproject.toml
      - uv.lock
      - setup_env.sh
      - bash_scripts/runpod_bootstrap.sh
      - scripts/check_runpod_environment.py
      - .github/workflows/runpod-image.yml

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate image metadata
        id: metadata
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/e2d2-runpod
          tags: |
            type=sha,prefix=sha-
            type=raw,value=cuda128-dev,enable=${{ github.ref == 'refs/heads/experimental' }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.runpod
          platforms: linux/amd64
          push: true
          tags: ${{ steps.metadata.outputs.tags }}
          labels: ${{ steps.metadata.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
